# Active Todo Queue

Mission: complete the runtime resize + host-behavior validation slice and close its follow-up validation loop.

1. Run Windows DAW smoke checks for loop playback, tempo automation, sample-rate/buffer-size changes, and project reload using `docs/DEVELOPMENT.md` protocol.
2. Run a Windows visual regression pass on the new toybox waveform sampling visual (steady tones, transients, overlay/split, and zoom extremes).
3. Select the next `docs/PRODUCT_SPEC.md` feature slice and write a focused active plan note before coding.

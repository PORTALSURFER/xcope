# Active Todo Queue

Mission: keep handoff clarity high and continue product-spec delivery in small verified slices.

1. Define the next feature slice from `docs/PRODUCT_SPEC.md` in a focused plan note under `docs/plans/active/`.
2. Implement that slice with unit tests and explicit error handling where relevant.
3. Run `bash scripts/ci_local.sh`, then update `MEMORY.md` and this queue to reflect the new current state.
